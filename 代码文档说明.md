# ComfyUI-LLMs 代码文档说明

## 项目概述

ComfyUI-LLMs 是一个功能强大的 ComfyUI 扩展，集成了多种大语言模型(LLM)和视觉语言模型(VLM)能力，为用户提供统一的接口和简单的配置方式。

### 核心功能

- 🤖 **多模型对话支持**：支持多种LLM模型的智能对话功能
- 🎯 **统一视觉接口**：集成多个视觉语言模型，提供统一的图像理解能力
- 🔄 **动态模型切换**：灵活切换不同的AI模型
- 🌐 **双语支持**：中英文双语界面
- ⚙️ **简单配置**：通过YAML文件轻松配置

---

## 项目结构

```
ComfyUI-LLMs/
├── __init__.py                      # 入口文件，定义节点映射
├── settings.py                      # 配置文件加载模块
├── settings.yaml.sample             # 配置文件模板
├── validate_config.py               # 配置验证工具
├── openai_client.py                 # OpenAI客户端封装
├── comfyui_llms_wrapper.py          # 向后兼容包装器
├── LLMs_Chat.py                     # 聊天节点实现
├── LLMs_Vision_Unified.py           # 统一视觉节点实现
├── LLMs_Vision_OpenAI.py            # OpenAI视觉处理
├── LLMs_Vison_GLM4.py               # GLM4视觉处理
├── LLMs_Vison_Ali.py                # 阿里视觉处理
├── LLMs_Vison_Gemini.py             # Gemini视觉处理
├── requirements.txt                 # Python依赖
├── README.md                        # 项目说明文档
├── CHANGELOG.md                     # 更新日志
└── examples/                        # 示例截图
```

---

## 核心模块详解

### 1. 入口模块 (__init__.py)

**功能**：ComfyUI插件的入口点，定义节点类映射和显示配置。

**核心代码**：

```python
from .LLMs_Chat import LLMs_Chat
from .LLMs_Vision_Unified import LLMs_Vision_Unified

NODE_CLASS_MAPPINGS = {
    "LLMs Chat": LLMs_Chat,
    "LLMs Vision Unified": LLMs_Vision_Unified
}

NODE_DISPLAY_NAME_MAPPINGS = {
    "LLMs Chat": "🤖 LLMs Chat | 智能对话",
    "LLMs Vision Unified": "🎯 LLMs Vision | 图像理解"
}

NODE_DISPLAY_CATEGORY_MAPPINGS = {
    "LLMs Chat": "🌟 LLMs",
    "LLMs Vision Unified": "🎭 LLMs"
}
```

**说明**：
- 导入两个核心节点类：`LLMs_Chat` 和 `LLMs_Vision_Unified`
- 定义节点类名到实际类的映射
- 配置节点的显示名称（支持emoji和中英文）
- 配置节点的分类显示

---

### 2. 配置管理模块 (settings.py)

**功能**：负责加载和管理配置文件，提供统一的配置访问接口。

**核心函数**：

#### load_settings()
```python
def load_settings():
    """加载配置文件，如果文件不存在则返回默认配置"""
    path = os.path.join(os.path.dirname(__file__), "settings.yaml")
    file_path = pathlib.Path(path)
    if not file_path.exists():
        return DEFAULT_SETTINGS['chatllmleoleexh']

    with open(path, 'r', encoding='utf-8') as settings:
        the_yaml = yaml.safe_load(settings)
    return the_yaml['chatllmleoleexh']
```

**说明**：
- 优先从 `settings.yaml` 加载配置
- 如果配置文件不存在，返回默认配置
- 使用YAML格式解析配置

#### get_chat_settings(section)
```python
def get_chat_settings(section: str = "default"):
    """获取聊天配置"""
    settings = load_settings()
    return settings['openai_compatible'][section]
```

**说明**：
- 获取指定section的聊天配置
- 默认返回 "default" 配置

#### get_vision_settings(model_type)
```python
def get_vision_settings(model_type: str):
    """获取视觉模型配置"""
    settings = load_settings()
    return settings['vision_models'].get(model_type)
```

**说明**：
- 根据模型类型获取视觉模型配置
- 支持的模型类型：openai, glm4, ali, gemini

---

### 3. 聊天节点 (LLMs_Chat.py)

**功能**：实现LLM对话功能，支持多种OpenAI兼容的模型。

**核心类**：

```python
class LLMs_Chat:
    """聊天模型节点类"""
    
    @classmethod
    def INPUT_TYPES(s):
        """定义节点的输入类型"""
        all_settings = load_settings()
        default_user_prompt = all_settings['example_user_prompt']
        available_apis = [a for a in all_settings['openai_compatible']]
        default_model = all_settings['openai_compatible']['default']['model']

        return {
            "required": {
                "api": (available_apis, {"default": "default"}),
                "model": (default_model, {"default": "gpt-3.5-turbo"}),
                "system_prompt": ("STRING", {
                    "default": "act as prompt generator...",
                    "multiline": True, "dynamicPrompts": False
                }),
                "user_prompt": ("STRING", {
                    "multiline": True,
                    "default": default_user_prompt
                }),
                "temperature": ("FLOAT", {
                    "default": 0.99, "min": 0.0, "max": 2.0, "step": 0.01,
                }),
            },
            "optional": {
                "top_p": ("FLOAT", {
                    "default": 1.0, "min": 0.001, "max": 1.0, "step": 0.01,
                }),
            }
        }
```

**输入参数说明**：
- `api`: API配置名称（从settings.yaml读取）
- `model`: 使用的模型名称
- `system_prompt`: 系统提示词，定义AI的角色和行为
- `user_prompt`: 用户输入的对话内容
- `temperature`: 温度参数（0.0-2.0），控制输出的随机性
- `top_p`: 核采样参数（0.001-1.0）

**核心方法**：

```python
def generate(self, api: str, model: str, temperature: float | None = None,
             top_p: float | None = None, user_prompt="", system_prompt=""):
    """生成对话响应"""
    system_content = system_prompt
    user_content = user_prompt

    response = OpenAiClient.complete(
        key=api,
        model=model,
        temperature=temperature,
        top_p=top_p,
        system_content=system_content,
        user_content=user_content)

    return (f'{response.choices[0].message.content}',)
```

**说明**：
- 调用OpenAiClient完成对话生成
- 返回模型生成的文本响应

---

### 4. 统一视觉节点 (LLMs_Vision_Unified.py)

**功能**：统一的视觉语言模型接口，支持多种VLM模型。

**核心类**：

```python
class LLMs_Vision_Unified:
    """统一的视觉模型节点"""
    
    def __init__(self):
        self.config = self._load_config()
        self.model_lists = {}
        # 预先加载所有模型类型的子模型列表
        for model_type in self.config['vision_models']:
            self.model_lists[model_type] = self.config['vision_models'][model_type]['model_list']
```

**支持的模型类型**：
- `openai`: OpenAI GPT-4V
- `glm4`: 智谱 GLM-4V
- `ali`: 阿里通义千问 VL
- `gemini`: Google Gemini Vision

**输入参数**：
- `image`: 图像输入（ComfyUI的IMAGE类型）
- `model_type`: 模型类型（openai/glm4/ali/gemini）
- `model`: 具体模型名称
- `prompt`: 图像描述提示词

**核心方法**：

#### _encode_image(image_tensor)
```python
def _encode_image(self, image_tensor):
    """将图像编码为base64字符串"""
    if isinstance(image_tensor, torch.Tensor):
        image_tensor = image_tensor.cpu()
        image = Image.fromarray(np.clip(image_tensor.squeeze().numpy() * 255, 0, 255).astype(np.uint8))
    elif isinstance(image_tensor, np.ndarray):
        image = Image.fromarray(np.clip(image_tensor * 255, 0, 255).astype(np.uint8))
    else:
        raise ValueError("不支持的图像格式")
    
    buffered = io.BytesIO()
    image.save(buffered, format="PNG")
    return base64.b64encode(buffered.getvalue()).decode()
```

**说明**：
- 将ComfyUI的图像张量转换为base64编码
- 支持torch.Tensor和numpy.ndarray格式
- 输出PNG格式的base64字符串

#### process_image(image, model_type, model, prompt)
```python
def process_image(self, image, model_type, model, prompt):
    """处理图像并返回描述"""
    model_config = self._get_vision_config(model_type)
    encoded_image = self._encode_image(image)
    
    # 验证所选模型是否属于当前模型类型
    if model not in self.model_lists[model_type]:
        available_models = ", ".join(self.model_lists[model_type])
        return (f"错误：所选模型 {model} 不属于 {model_type} 类型。可用的模型有：{available_models}",)
    
    # 根据不同模型类型调用相应的API
    try:
        if model_type == "openai":
            from .LLMs_Vision_OpenAI import process_openai
            return (process_openai(encoded_image, prompt, model_config),)
        elif model_type == "glm4":
            from .LLMs_Vison_GLM4 import process_glm4
            return (process_glm4(encoded_image, prompt, model_config),)
        elif model_type == "ali":
            from .LLMs_Vison_Ali import process_ali
            return (process_ali(encoded_image, prompt, model_config),)
        elif model_type == "gemini":
            from .LLMs_Vison_Gemini import process_gemini
            return (process_gemini(encoded_image, prompt, model_config),)
        else:
            return (f"不支持的模型类型: {model_type}",)
    except Exception as e:
        return (f"处理图像时出错: {str(e)}",)
```

**说明**：
- 验证模型选择的合法性
- 根据模型类型分发到对应的处理函数
- 统一的错误处理机制

---

### 5. OpenAI客户端 (openai_client.py)

**功能**：封装OpenAI API调用，提供简洁的接口。

**核心类**：

```python
class OpenAiClient:
    @staticmethod
    def create_client(key: str = "default"):
        """创建OpenAI客户端"""
        settings = get_chat_settings(key)
        the_client = OpenAI(
            base_url=settings['api_base'],
            api_key=settings['api_key'],
            organization=settings['organisation'],
        )
        return the_client
```

**核心方法**：

#### complete()
```python
@staticmethod
def complete(key: str, model: str, temperature: float, top_p: float, 
             system_content: str, user_content: str):
    """完成对话生成"""
    errors = validation(temperature, top_p)
    if errors:
        error_report = "\n".join([e for e in errors])
        raise RuntimeError(f"There were problems with the parameters:\n{error_report}")
    
    the_client = OpenAiClient.create_client(key)
    response = the_client.chat.completions.create(
        model=model,
        temperature=temperature,
        top_p=top_p,
        messages=[
            {"role": "system", "content": system_content},
            {"role": "user", "content": user_content}
        ]
    )
    return response
```

**参数验证**：

```python
def validation(temperature: float, top_p: float = None) -> list[str]:
    """验证参数合法性"""
    errors_list = []
    if temperature is None and top_p is None:
        errors_list.append('Must contain a temperature or a top_p')
    if top_p < 0 or top_p > 1:
        errors_list.append('top_p must be a number between 0 and 1')
    if temperature < 0 or temperature > 2:
        errors_list.append('Temperature should be a value between 0.0 and 2.0')
    return errors_list
```

---

### 6. OpenAI视觉处理 (LLMs_Vision_OpenAI.py)

**功能**：处理OpenAI GPT-4V视觉模型的图像理解请求。

**核心函数**：

```python
def process_openai(encoded_image, prompt, config):
    """处理图像并返回OpenAI视觉模型的响应"""
    try:
        # 初始化客户端
        client = OpenAI(
            api_key=config['api_key'],
            base_url=config['api_base']
        )
        
        # 准备消息
        messages = [
            {
                "role": "user",
                "content": [
                    {
                        "type": "text",
                        "text": prompt
                    },
                    {
                        "type": "image_url",
                        "image_url": {
                            "url": f"data:image/png;base64,{encoded_image}"
                        }
                    }
                ]
            }
        ]
        
        # 调用API
        response = client.chat.completions.create(
            model=config['model_list'][0],
            messages=messages,
            max_tokens=1000
        )
        
        # 返回结果
        return response.choices[0].message.content
        
    except Exception as e:
        return f"OpenAI视觉处理出错: {str(e)}"
```

**说明**：
- 使用OpenAI的chat.completions接口
- 支持图像URL（base64编码）
- 返回模型生成的文本描述

---

### 7. GLM4视觉处理 (LLMs_Vison_GLM4.py)

**功能**：处理智谱GLM-4V视觉模型的图像理解请求。

**核心函数**：

```python
def process_glm4(encoded_image, prompt, config):
    """处理图像并返回GLM4的响应"""
    try:
        # 初始化客户端
        client = ZhipuAI(api_key=config['api_key'])
        
        # 准备消息
        messages = [
            {
                "role": "user",
                "content": [
                    {
                        "type": "text",
                        "text": prompt
                    },
                    {
                        "type": "image_url",
                        "image_url": {
                            "url": f"data:image/png;base64,{encoded_image}"
                        }
                    }
                ]
            }
        ]
        
        # 调用API
        response = client.chat.completions.create(
            model=config['model_list'][0],
            messages=messages,
            max_tokens=1000,
            temperature=0.8
        )
        
        # 返回结果
        return response.choices[0].message.content
        
    except Exception as e:
        return f"GLM4处理出错: {str(e)}"
```

**说明**：
- 使用智谱AI的ZhipuAI客户端
- 接口格式与OpenAI兼容
- 支持温度参数控制

**额外功能**：
- 包含已废弃的GLM4专用聊天节点 `LLMs_Chat_GLM4_Only`
- 提供纯文本对话功能

---

### 8. 阿里视觉处理 (LLMs_Vison_Ali.py)

**功能**：处理阿里通义千问视觉模型的图像理解请求。

**核心函数**：

```python
def process_ali(encoded_image, prompt, config):
    """处理图像并返回阿里视觉模型的响应"""
    try:
        # 设置API密钥
        dashscope.api_key = config['api_key']
        
        # 准备消息
        messages = [{
            'role': 'user',
            'content': [
                {'image': f'data:image/png;base64,{encoded_image}'},
                {'text': prompt}
            ]
        }]
        
        # 调用API
        response = MultiModalConversation.call(
            model=config['model_list'][0],
            messages=messages
        )
        
        # 检查响应
        if response.status_code == 200:
            # 从响应中提取文本内容
            if hasattr(response.output, 'choices') and len(response.output.choices) > 0:
                choice = response.output.choices[0]
                if hasattr(choice, 'message') and hasattr(choice.message, 'content'):
                    if isinstance(choice.message.content, list):
                        for content in choice.message.content:
                            if isinstance(content, dict) and 'text' in content:
                                return content['text']
                    else:
                        return choice.message.content
            
            if hasattr(response.output, 'text'):
                return response.output.text
            
            return str(response.output)
        else:
            return f"阿里视觉模型调用失败: {response.status_code}"
            
    except Exception as e:
        return f"阿里视觉处理出错: {str(e)}"
```

**说明**：
- 使用阿里云的dashscope SDK
- 支持多模态对话
- 复杂的响应解析逻辑

---

### 9. Gemini视觉处理 (LLMs_Vison_Gemini.py)

**功能**：处理Google Gemini视觉模型的图像理解请求。

**核心函数**：

```python
def process_gemini(encoded_image, prompt, config):
    """处理图像并返回Gemini视觉模型的响应"""
    try:
        # 配置API
        genai.configure(api_key=config['api_key'])
        
        # 初始化模型
        model = genai.GenerativeModel(config['model_list'][0])
        
        # 解码图像
        image_data = base64.b64decode(encoded_image)
        image = PIL.Image.open(BytesIO(image_data))
        
        # 生成响应
        response = model.generate_content([prompt, image])
        
        # 返回结果
        return response.text
        
    except Exception as e:
        return f"Gemini处理出错: {str(e)}"
```

**说明**：
- 使用Google的generativeai SDK
- 直接使用PIL.Image对象
- 简洁的API调用方式

**额外功能**：
- 包含已废弃的Gemini专用节点 `LLMs_Vison_Gemini`
- 支持流式输出

---

### 10. 配置验证工具 (validate_config.py)

**功能**：验证配置文件的正确性，帮助用户排查配置问题。

**核心函数**：

```python
def validate_config():
    """验证配置文件"""
    print("🔍 正在验证配置文件...")
    
    try:
        # 1. 检查配置文件是否存在
        config_path = os.path.join(os.path.dirname(__file__), "settings.yaml")
        if not os.path.exists(config_path):
            print("❌ 错误: settings.yaml 文件不存在")
            print("💡 请先运行: cp settings.yaml.sample settings.yaml")
            return False
        
        # 2. 测试配置加载
        config = load_settings()
        print("✅ 配置文件加载成功")
        
        # 3. 验证必需的配置节
        required_sections = ['openai_compatible', 'vision_models', 'example_user_prompt']
        for section in required_sections:
            if section not in config:
                print(f"❌ 错误: 缺少必需的配置节 '{section}'")
                return False
        print("✅ 所有必需的配置节都存在")
        
        # 4. 验证聊天配置
        chat_config = get_chat_settings()
        print(f"📝 聊天配置:")
        print(f"   - API端点: {chat_config['api_base']}")
        print(f"   - API密钥: {chat_config['api_key'][:10]}...")
        print(f"   - 可用模型数量: {len(chat_config['model'])}")
        
        # 5. 验证视觉模型配置
        vision_types = ['openai', 'glm4', 'ali', 'gemini']
        print(f"🎯 视觉模型配置:")
        for vtype in vision_types:
            vision_config = get_vision_settings(vtype)
            if vision_config:
                print(f"   - {vtype}: {len(vision_config['model_list'])} 个模型")
            else:
                print(f"   - {vtype}: 未配置")
        
        # 6. 检查API密钥是否为默认值
        if chat_config['api_key'] == 'sk-xxxxxxx':
            print("⚠️  警告: 聊天API密钥仍为默认值，请更新为实际密钥")
        
        for vtype in vision_types:
            vision_config = get_vision_settings(vtype)
            if vision_config and vision_config.get('api_key', '').startswith(('sk-xxxxx', 'xxxxxx', 'AIxxxxxx')):
                print(f"⚠️  警告: {vtype} API密钥仍为默认值，请更新为实际密钥")
        
        print("\n🎉 配置验证完成！")
        return True
        
    except Exception as e:
        print(f"❌ 配置验证失败: {str(e)}")
        return False
```

**验证内容**：
- 配置文件存在性检查
- 配置文件结构验证
- 必需配置节检查
- API端点和密钥验证
- 模型列表验证
- 默认值警告

---

### 11. 向后兼容包装器 (comfyui_llms_wrapper.py)

**功能**：提供向后兼容的API设置获取函数。

```python
from .settings import get_chat_settings

def api_settings(section: str = "default"):
    """保持向后兼容的API设置获取函数"""
    settings = get_chat_settings(section)
    return settings['api_base'], settings['api_key'], settings['organisation']
```

**说明**：
- 保留旧版本的API接口
- 确保与旧代码的兼容性

---

## 配置文件说明

### settings.yaml 结构

```yaml
chatllmleoleexh:
  # OpenAI兼容接口配置
  openai_compatible:
    default:
      api_base: "http://your-api-endpoint:3200/v1"  # API端点
      organisation: "NONE"                          # 组织ID（可选）
      api_key: "your-api-key"                      # API密钥
      model:                                       # 支持的模型列表
        - "gpt-3.5-turbo"
        - "gpt-4"
        - "glm-4"
        - "chatglm_pro"
        - "ERNIE-Bot-4"
        - "qwen-turbo-internet"

  # 视觉模型配置
  vision_models:
    openai:
      api_key: "your-openai-key"
      api_base: "http://your-api-endpoint:3200/v1"
      model_list:
        - "gpt-4-vision-preview"
    
    glm4:
      api_key: "your-glm4-key"
      model_list: 
        - "glm-4v"
        - "glm-4"
    
    ali:
      api_key: "your-ali-key"
      model_list:
        - "qwen-vl-plus"
        - "qwen-vl-max"
    
    gemini:
      api_key: "your-gemini-key"
      model_list:
        - "gemini-pro-vision"

  # 示例用户提示词
  example_user_prompt: "your user prompt here"

  # 提示词模板配置
  prompt_templates:
    default:
      system: >-
        act as prompt generator, I will give you text and you describe an image 
        that matches that text in details, answer with one response only.
      prefix: >-
        make a list of the things you see. do not explain why you see them.
      suffix: >-
        describe what you see in your mind.
```

### 配置项说明

#### openai_compatible
- `api_base`: API端点URL
- `organisation`: 组织ID（可选）
- `api_key`: API密钥
- `model`: 支持的模型列表

#### vision_models
每个模型类型包含：
- `api_key`: 该模型的API密钥
- `api_base`: API端点（可选）
- `model_list`: 该模型类型支持的模型列表

#### prompt_templates
- `system`: 系统提示词模板
- `prefix`: 前缀提示词模板
- `suffix`: 后缀提示词模板

---

## 依赖说明

### requirements.txt

```
openai>=1.0.0              # OpenAI API客户端
zhipuai>=2.0.0             # 智谱AI客户端
dashscope>=1.13.0          # 阿里云客户端
google-generativeai>=0.3.0 # Google Gemini客户端
Pillow>=10.0.0             # 图像处理
PyYAML>=6.0.1              # YAML配置解析
```

### 依赖说明

1. **openai**: OpenAI官方Python SDK，用于调用OpenAI API及兼容接口
2. **zhipuai**: 智谱AI官方SDK，用于调用GLM系列模型
3. **dashscope**: 阿里云SDK，用于调用通义千问系列模型
4. **google-generativeai**: Google官方SDK，用于调用Gemini模型
5. **Pillow**: Python图像处理库，用于图像编码和格式转换
6. **PyYAML**: YAML文件解析库，用于读取配置文件

---

## 使用流程

### 1. 安装步骤

```bash
# 1. 进入ComfyUI的custom_nodes目录
cd ComfyUI/custom_nodes

# 2. 克隆仓库
git clone https://github.com/leoleexh/ComfyUI-LLMs

# 3. 进入项目目录
cd ComfyUI-LLMs

# 4. 安装依赖
pip install -r requirements.txt

# 5. 复制配置文件
cp settings.yaml.sample settings.yaml

# 6. 编辑配置文件，填入API密钥
# 使用文本编辑器打开 settings.yaml 并修改

# 7. 验证配置
python validate_config.py
```

### 2. 使用聊天功能

1. 在ComfyUI中打开节点菜单
2. 找到 `🌟 LLMs` 分类
3. 添加 `🤖 LLMs Chat | 智能对话` 节点
4. 配置参数：
   - 选择API配置
   - 选择模型
   - 设置系统提示词
   - 输入用户提示词
   - 调整temperature和top_p参数
5. 运行节点获取响应

### 3. 使用图像理解功能

1. 在ComfyUI中打开节点菜单
2. 找到 `🎭 LLMs` 分类
3. 添加 `🎯 LLMs Vision | 图像理解` 节点
4. 连接图像输入（来自其他ComfyUI节点）
5. 配置参数：
   - 选择模型类型（openai/glm4/ali/gemini）
   - 选择具体模型
   - 输入描述提示词
6. 运行节点获取图像描述

---

## API密钥获取

### OpenAI
- 官网：https://platform.openai.com/
- 获取方式：注册账号后在API Keys页面创建

### 智谱GLM
- 官网：https://open.bigmodel.cn/
- 获取方式：注册账号后在API管理页面创建

### 阿里通义千问
- 官网：https://dashscope.aliyun.com/
- 获取方式：注册账号后在API-KEY管理页面创建

### Google Gemini
- 官网：https://makersuite.google.com/
- 获取方式：注册账号后在API Keys页面创建

---

## 技术架构

### 数据流向

#### 聊天流程
```
用户输入 → LLMs_Chat节点 → OpenAiClient → API调用 → 响应返回 → 输出文本
```

#### 图像理解流程
```
图像输入 → LLMs_Vision_Unified节点 → 图像编码 → 模型分发 → API调用 → 响应返回 → 输出描述
```

### 模块关系图

```
__init__.py (入口)
    ├── LLMs_Chat.py (聊天节点)
    │   └── openai_client.py (OpenAI客户端)
    │       └── settings.py (配置管理)
    │
    └── LLMs_Vision_Unified.py (统一视觉节点)
        ├── LLMs_Vision_OpenAI.py (OpenAI视觉)
        ├── LLMs_Vison_GLM4.py (GLM4视觉)
        ├── LLMs_Vison_Ali.py (阿里视觉)
        ├── LLMs_Vison_Gemini.py (Gemini视觉)
        └── settings.py (配置管理)
```

### 设计模式

1. **策略模式**：不同的视觉模型处理函数作为策略，根据模型类型动态选择
2. **工厂模式**：OpenAiClient.create_client() 根据配置创建客户端实例
3. **单例模式**：配置文件加载使用单例模式，避免重复读取
4. **适配器模式**：统一视觉节点适配不同模型的API接口

---

## 错误处理

### 常见错误及解决方案

#### 1. 配置文件不存在
```
错误：settings.yaml 文件不存在
解决：cp settings.yaml.sample settings.yaml
```

#### 2. API密钥错误
```
错误：Authentication failed
解决：检查settings.yaml中的API密钥是否正确
```

#### 3. 模型不支持
```
错误：所选模型不属于该类型
解决：检查模型名称是否在配置的model_list中
```

#### 4. 网络连接失败
```
错误：Connection timeout
解决：检查网络连接，或配置代理
```

#### 5. 图像格式错误
```
错误：不支持的图像格式
解决：确保输入是ComfyUI的IMAGE类型张量
```

---

## 性能优化建议

1. **模型选择**：
   - 对话任务：使用GPT-3.5-turbo或GLM-4
   - 图像理解：根据需求选择合适的视觉模型
   - 成本敏感：选择价格较低的模型

2. **参数调优**：
   - temperature: 0.7-1.0 适合创意生成，0.2-0.5 适合精确回答
   - top_p: 0.9-1.0 适合多样性，0.7-0.9 适合稳定性

3. **网络优化**：
   - 使用稳定的网络环境
   - 配置API代理（如需要）
   - 考虑使用CDN加速

4. **缓存策略**：
   - 对重复的提示词结果进行缓存
   - 避免不必要的API调用

---

## 扩展开发

### 添加新的视觉模型

1. 创建新的处理模块（如 `LLMs_Vision_NewModel.py`）
2. 实现 `process_newmodel(encoded_image, prompt, config)` 函数
3. 在 `settings.yaml` 中添加模型配置
4. 在 `LLMs_Vision_Unified.py` 中添加模型类型分支
5. 在 `__init__.py` 中更新节点映射（如需要）

### 添加新的聊天模型

1. 在 `settings.yaml` 的 `openai_compatible` 中添加新的API配置
2. 在模型列表中添加支持的模型名称
3. 如需特殊处理，可在 `openai_client.py` 中扩展

---

## 注意事项

1. **API密钥安全**：
   - 不要将API密钥提交到版本控制系统
   - 定期更换API密钥
   - 使用环境变量存储敏感信息（可选）

2. **API调用限制**：
   - 注意各平台的API调用频率限制
   - 合理控制请求速率
   - 监控API使用量

3. **网络稳定性**：
   - 确保网络连接稳定
   - 考虑添加重试机制
   - 处理网络超时情况

4. **成本控制**：
   - 选择合适的模型和参数
   - 监控API使用成本
   - 优化提示词长度

---

## 版本历史

详见 [CHANGELOG.md](CHANGELOG.md)

### 主要版本

- **v1.0.0** (2024-01-11): 重构项目，统一视觉接口
- **v0.0.1** (2023-12-20): 初始版本发布

---

## 许可证

MIT License

---

## 贡献指南

欢迎提交Issue和Pull Request！

### 开发环境设置

```bash
# 克隆仓库
git clone https://github.com/leoleexh/ComfyUI-LLMs

# 创建虚拟环境
python -m venv venv
source venv/bin/activate  # Linux/Mac
venv\Scripts\activate     # Windows

# 安装开发依赖
pip install -r requirements.txt
```

### 代码规范

- 遵循PEP 8代码风格
- 添加必要的文档字符串
- 保持代码简洁清晰
- 添加适当的错误处理

---

## 联系方式

- GitHub: https://github.com/leoleexh/ComfyUI-LLMs
- Issues: https://github.com/leoleexh/ComfyUI-LLMs/issues

---

## 附录

### A. 完整配置示例

参见 [settings.yaml.sample](settings.yaml.sample)

### B. 节点参数参考

#### LLMs Chat节点
- `api`: API配置名称
- `model`: 模型名称
- `system_prompt`: 系统提示词
- `user_prompt`: 用户提示词
- `temperature`: 温度参数 (0.0-2.0)
- `top_p`: 核采样参数 (0.001-1.0)

#### LLMs Vision节点
- `image`: 图像输入
- `model_type`: 模型类型
- `model`: 具体模型
- `prompt`: 描述提示词

### C. 支持的模型列表

#### OpenAI兼容
- gpt-3.5-turbo
- gpt-3.5-turbo-16k
- gpt-4
- gpt-4-0613
- gpt-4-1106-preview

#### 视觉模型
- OpenAI: gpt-4-vision-preview
- GLM4: glm-4v, glm-4
- 阿里: qwen-vl-plus, qwen-vl-max
- Gemini: gemini-pro-vision

---

*文档最后更新：2024-01-15*
